version: "3"

dotenv: ["../../.env"]

includes:
  zenml: scripts/tasks/zenml/Taskfile.yml

tasks:
  ruff-format:
    cmds:
      - uvx ruff format
  
  integrations:
    cmds:
      - task: zenml:setup:all-integrations  
